# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Claude Code local (personal) settings — the shared CLAUDE.md is tracked
.claude/settings.local.json

# Dependencies / build (none today, but keep the repo clean if added later)
/vendor/
/node_modules/

# Logs
*.log
